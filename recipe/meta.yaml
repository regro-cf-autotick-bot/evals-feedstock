{% set name = "evals" %}
{% set version = "3.0.1.post1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/openai/evals/archive/refs/tags/{{ version }}.tar.gz
  sha256: 9510c6cb76062a9d3a842698181c0a785fe57e174e61537734ef1d0c83d83428

build:
  entry_points:
    - oaieval = evals.cli.oaieval:main
    - oaievalset = evals.cli.oaievalset:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - aiolimiter
    - anthropic
    - beartype >=0.12.0
    - chess
    - dacite
    - datasets
    - docker-py
    - evaluate
    - flask
    - google-generativeai
    - gymnasium
    - jiwer
    - langchain
    - networkx
    - numexpr
    - playwright
    - pytest
    - seaborn
    - spacy-universal-sentence-encoder
    - statsmodels
    - types-pyyaml
    - types-tqdm
    - zstandard
    - python >={{ python_min }}
    - mypy
    - openai >=1.0.0
    - tiktoken
    - blobfile
    - backoff
    - numpy
    - snowflake-connector-python
    - pandas
    - fire
    - pydantic
    - tqdm
    - nltk
    - filelock
    - mock
    - langdetect
    - termcolor
    - lz4
    - pyzstd
    - pyyaml
    - sacrebleu
    - matplotlib-base
    - setuptools-scm

test:
  imports:
    - evals
  commands:
    - pip check
    - oaieval --help
    - oaievalset --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/openai/evals
  summary: Evals is a framework for evaluating OpenAI models and an open-source registry of benchmarks.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - BastianZim
